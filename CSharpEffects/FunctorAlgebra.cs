using System;
using System.Collections;
using System.Collections.Generic;
using System.Diagnostics;
using System.Linq;
using System.Linq.Expressions;
using System.Text;
using System.Threading.Tasks;

namespace CSharpEffects
{
    //interface Functor<F>
    //{
    //    F<B> Map<A, B>(Func<A, B> fn, F<A> source);
    //}

    //class LazyFunctor : Functor<Lazy>
    //{
    //    public Lazy<B> Map<A, B>(Func<A, B> fn, Lazy<A> source) 
    //        => LazyAlgebra.Map(fn, source);
    //}
}
